# Config

Environment configuration for test runs: base URLs, timeouts, environment
name (staging/prod), and other run-time settings. Keep secrets out of this
folder — use environment variables / a local untracked .env instead.
