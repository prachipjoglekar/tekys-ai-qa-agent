# Security Tests

Automated, non-destructive checks for common web security issues on
tekyz.com: security headers, TLS/cert config, exposed files, mixed content,
cookie flags, and similar passive checks.

All security tests must be read-only against production — no scanning
techniques that could modify data, submit forms with side effects, or
degrade site availability.

No tests implemented yet — placeholder only.
